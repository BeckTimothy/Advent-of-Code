let input = require("fs").readFileSync("../challenge-1/input.txt", { encoding: "utf-8", flag: 'r'}).trim();
//x,y,z
//if any input is z[0],x[0],y[0], or z[length],x[length],y[length] add items to both ends of every item in that plane




